/* const cursor = document.querySelector("div.cursor");
let X = 600;
let Y = 80;
cursor.style.left = X + "px";
cursor.style.top = Y + "px";


/* cursor.addEventListener("mousemove", (move) => {
 let X = 0;
 let Y = 0;
 cursor.style.left = `${move.clientX- 30 + "px"}`;
 cursor.style.top = `${move.clientY- 30 + "px"}`;
}) */

